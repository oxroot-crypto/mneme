# cargo-mutants 配置(设计 14 §7):存活变异体 = 约束遗漏测试。
#
# 只变异生产源码;测试/基准/fuzz/文档不参与。
exclude_globs = [
  "benches/**",
  "fuzz/**",
  "docs/**",
  "tests/**",
  "examples/**",
]

# 每个变异体在基线耗时上的放大倍数(慢集成测试用);超时即视为检测到。
timeout_multiplier = 3.0
