# mdBook 构建输出
/book/
/book.tar.gz

# mdbook-mermaid 生成资源（由 `mdbook-mermaid install .` 生成）
/mermaid.min.js
/mermaid-init.js

# Rust
/target/
**/*.rs.bk
*.pdb

# 对比基准独立 workspace 的构建产物
/comparison/target/
# comparison 的自动运行产物(原始 JSON 与自动 Markdown);结论报告是手写的
# comparison/README.md,可追踪的数值以报告表格为准。
/comparison/results/
/comparison/results-*/

# 编辑器 / IDE
.idea/
.vscode/
*.iml
*.swp
*.swo
*~

# 操作系统
.DS_Store
Thumbs.db
desktop.ini

# 环境 / 本地配置
.env
.env.local
*.local

# 日志与临时文件
*.log
/tmp/

# CI 跨 feature fixture(GitLab/GitHub 在仓库目录下生成)
/.ci/
.codegraph/